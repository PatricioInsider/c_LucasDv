#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    int a,b,c,x ;
    cout << "Ingrese el cateto a :  ";
    cin >> a << end;
    cout << "Ingrese el cateto b :  ";
    cin >> b << endl;

    c = sqrt(pow(a,2)*pow(b,2));

    cout << "Hipotenusa del triangulo: " << c << endl;
    cout << "Fin del programa";

    return 0;
}
